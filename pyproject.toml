[project]
name = "sovereign-ai-backend"
version = "1.0.0"
description = "Sovereign AI Workbench FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]",
]

[tool.vercel]
entrypoint = "fast_api.app:app"